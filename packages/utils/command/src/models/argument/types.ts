export interface Options {
  description: string;
  name: string;
  required?: boolean;
  spread?: boolean;
  warning?: string;
}

export interface Serial {
  description: string;
  name: string;
  required: boolean;
  spread: boolean;
  warning?: string;
}
